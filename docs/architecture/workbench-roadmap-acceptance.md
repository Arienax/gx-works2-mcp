# 工程工作台验收记录 · 2026-09-12

本次从 `Arienax/gxworks-agent` 的 `feat/integration-onboarding` 最新提交 `1240ef7` 克隆，在 `codex/roadmap-integration-acceptance` 分支完成修改。原工作副本及其未提交修改保留。

## 已实现范围

- 程序查看器：网络选择、地址和注释搜索、读写引用跳转、缩放与无模型重绘。
- 问题卡片：绑定报告版本、网络和证据，创建复现测试并回链已保存方案。
- 修改约束：网络及地址范围在候选创建和保存时校验；保存版本展示实际影响摘要。
- 仿真工作台：步骤编辑、不可变方案、运行观测回放、需求和问题关联。完成后台任务后自动更新方案列表，保留正在编辑的草稿。
- FBD：草稿通过写入与回读校验后更新图形；原生验证记录绑定已保存 GXW 指纹，区分操作员报告与自动验证。
- 首个工程引导、面向用户的 Codex 接入说明、可阅读和下载的工程交付摘要。
- 高级维护下的真实 PLC 只读入口：默认关闭，限定逻辑站、地址、有效期和适配器指纹；独立读取进程无写入、强制或 CPU 状态控制接口。

保持最新分支的直接生成/修改自动保存行为，以及 `generation_structural` 单次生成路径；未增加隐藏的生成语义重试或二次保存确认。独立 MCP 候选仍保持 `confirmation_required`。

## 真实 API 与浏览器验收

使用用户授权的真实 DeepSeek 密钥，通过 `https://api.deepseek.com` 和 `deepseek-v4-flash` 调用。密钥经无回显输入进入测试进程，仅存内存；未写入工作区配置、证据或 Git。每个会话限制请求数量、超时和目标地址。未用模拟响应替代以下模型调用。

浏览器操作通过 Computer Use / CUA 完成，操作对象为单独的本地测试服务和测试工程。

| 场景 | 实际结果 |
| --- | --- |
| 新建工程、分析需求、确认规格、生成 | 两路 `LD X0 → OUT Y0`、`LD X1 → OUT Y1` 自动保存为 v0001 |
| 地址搜索与引用 | 搜索 Y1，查看写入引用，跳转 N0002；缩放和重绘正常，重绘未调用模型 |
| 限制 N0001 的局部修改 | v0002 仅把 X0 改为 `LDI X0`；N0002、注释保留，摘要显示 N0001 和 X0/Y0 |
| 真实评审 | 第一次非法 JSON 被标为 partial；明确点击重试后，两个评审角色完成，报告指出 N0001 与原规格的极性差异 |
| 问题 → 网络 → 复现方案 | 正确打开 v0002/N0001；保存期望 `X0=0 → Y0=0` 的复现测试后，问题卡片显示可打开的关联方案 |
| 手工仿真方案 | 初始输入、100 ms 时刻、Y0/Y1 断言和需求关联可保存为新方案；未执行显示未运行 |
| 真实生成测试方案 | 修复后作业 completed；再次生成的新方案自动出现在列表，可以打开编辑，无需刷新页面 |
| FBD 草稿与保存 | 转换得到 v0003；将 Y0 改为 Y2，保存前图形已同步且原生记录表单禁用；保存得到 v0004 |
| 原生记录与交付 | 如实保存“未执行原生编译”的待确认记录；交付页显示该状态、版本指纹、规格摘要和表格 |
| Codex 接入提示 | 页面使用连接步骤、当前工程任务示例和结果检查说明；移除对话式修改记录措辞 |

两轮共 **11 次真实模型响应**，供应商用量合计输入 **112,118**、输出 **20,359**、总计 **132,477 tokens**。第一轮工程 `b9fff271e6d0`，第二轮修复复验工程 `be361b3fbd06`。最后的交付排版复验使用第一轮实际工程的隔离副本，未额外调用模型。

关键记录：

- 完整评审：`report_20328b0427ec45bbbabd1a5e344d541a`。
- 问题关联复现方案：`testplan_20260912T100146_e8cf6b4b`。
- 第一轮测试方案最初已持久化，但作业结果封装失败；修复读取 `binding.plan_id` 后，以第二轮真实 API 重新验收。原失败历史未被改写。

## 子智能体执行的真实 Codex MCP 验收

Codex CLI `0.153.4` 通过真实 `codex exec` 子进程启动仓库 MCP launcher，退出码 0。使用临时命令行配置和四个只读工具白名单，未修改用户 Codex 配置。

五次实际工具回执均为 completed、无错误：`get_current_project`、`get_current_program_info`、两次 `read_network`、`get_diagnostics`。活动版本已经变为 v0002，读取仍精确绑定 `b9fff271e6d0/v0001`。IR SHA-256 为 `11fc257d20a56742b480338ba64833e8bc823f6bd82533b4d62216f2059b0163`，两网络符合 v0001，12 条静态规则的错误/警告/提示均为 0。协议工具目录为 12 项，不含低层设备写入工具。

## 验收中修复的问题

- 禁止结构中的 `vfd_multi_speed` 被误识别成硬件需求，导致普通开关控制被强制补问变频器参数。
- 仿真方案保存后错误读取顶层 `plan_id`，导致已保存方案的作业显示失败。
- 已保存版本显示“候选待确认”、已有程序仍显示首次生成引导、后台方案完成后列表未更新。
- 多服务环境中，MCP 连接测试在检查服务归属前改写另一服务的工程绑定。
- 分支基线的 MCP 可选依赖边界、凭据发现隔离、翻译及构建链检查失败。

## 验证边界与证据

最终全量 Python 回归为 **1767 passed、3 skipped**（75.65 秒，另有一条依赖弃用提示）；前端生产构建、MCP 协议烟测和差异检查通过。MCP 烟测还确认独立候选返回 `confirmation_required`，工程未被写入。

三项环境跳过包括：GX Works2 当前有打开的工程、没有可用的现场操作摘要、Windows 环境不能创建符号链接。原生 GX 编译、GX Simulator2 实际执行、真实 PLC 连接和读写均未进行；波形回放有自动化数据绑定检查，但本轮没有真实仿真波形可验收。不得将软件流程通过解释为现场运行通过。

本地证据保存在 `D:\download\gx\gxworks-acceptance-20260912`，包括两轮实际工程、去除正文和凭据的模型诊断元数据、`live-api-summary.json` 与 `codex-mcp-verified-summary.json`。原始 Codex 工具回执由子智能体保存在独立临时目录。测试证据和密钥均不加入源代码提交。
