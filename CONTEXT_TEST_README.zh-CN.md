# GXWorks Agent 提示词策略测试版

基底：`fix/generation-result-refresh@c6bc6ef7c8740d63bca1adb650e0264d0ffd8970`。
此测试版保留该分支的生成修复、自动保存、受阻候选预览、结果刷新和重绘逻辑。在此基础上接入上下文策略；不修改 PLC 校验器、GXW 写入器、审批模式或真实设备边界。

## 启动

解压整个 Windows x64 发布目录到新文件夹，不覆盖旧安装目录。直接双击 `start-adaptive.cmd` 测试优化策略；双击 `start-legacy.cmd` 进行原策略对照。`START_CONTEXT_TEST.cmd` 可选择全部六种策略。

无需安装 Python、Node.js 或 Qt。GX/MX 软件及其合法许可仍由使用者提供。

测试启动器将工作区固定在 `%LOCALAPPDATA%\GXWorks-Agent\ContextPolicyTests\refresh-c6bc6ef\<策略>`，不会自动选择或迁移你原来的工作区。普通 `start-web.cmd` 保留原版行为，首次测试推荐使用上述专用入口。

模型配置仍使用原有设置模块。包内没有 API Key；必要时在界面配置你自己的模型。通过界面执行生成/分析会调用所选 API 并可能产生费用。仅打开启动器不会调用模型、GX 或 PLC。包内网关只作为文件提供，不会随启动器自动启动。

## 策略

- `adaptive`：移除无条件完整控制范例；遇到明确查证信号时自动检索。
- `legacy`：保留原先提示词和检索行为。
- `minimal`：无可选手册片段和控制范例；工程状态、私有协议和完整型号资料仍保留。
- `manual`：手册增强，不提供控制范例。
- `examples`：仅匹配且适用的范例，不自动检索手册。
- `combined`：匹配范例与手册增强。

源代码默认仍为 `legacy`；优化策略由专用启动器在自己的进程中设置。切换策略时要停止原服务再用相应入口启动。已提交任务使用提交时的策略快照。

## 如何对照

先测试普通梯形图生成/修改和诊断，不要把独立 FBD 或 Agent/MCP 路径混入同一统计。原生 FBD 对象生成保留分支原路径；显式 Agent/MCP 检索没有被本次策略关闭。

不同策略请使用相同的模型版本、参数、确认规格和工程基线。测试现有分支的“生成后查看程序”“刷新结果/重绘梯形图”“受阻候选不可接受”等行为是否保持。记录完整任务的通过情况和返工次数，不只比较提示词长度。

## 证据与限制

`build-info.json` 标明基底、测试构建提交和源码补丁摘要。`context-changes.patch` 可审查本次源码改动；四处运行时文件的接入由带前后哈希验证的构建脚本应用，不等同于未处理的分支文件。构建流程运行 Windows 回归、六策略上下文检查、浏览器端生成交付测试，以及解压后的 EXE 烟测；具体结果见配套 JSON/XML 证据。

固定模型回复验证流程衔接，不代表在线模型正确率。未进行真实 GX 编译、原生仿真或物理 PLC 验收。发布包未代码签名；不要为运行它关闭系统安全防护，应核对来源和 SHA-256。

本次仍保留基底的知识资料，不进行版权、许可证或资料分发架构改造。
